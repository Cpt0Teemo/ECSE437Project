#include <stdio.h>
#include <stdlib.h>
#include <string.h>

double sin(double rad){
    return (sin(rad));
}

double cos(double rad){
    return (cos(rad));
}

double tan(double rad){
    return (sin(rad)/cos(rad));
}